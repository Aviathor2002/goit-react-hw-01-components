import { UserInfo } from './UserInfo';
import PropTypes from 'prop-types';

export const ProfileCard = () => {
  return (
    <div>
      <UserInfo />
    </div>
  );
};

// key={user.tag}
// username={user.username}
// tag={user.tag}
// location={user.location}
// avatar={user.avatar}
// stats={user.stats}
//
{
  /* // ProfileCard.PropTypes = { key: PropTypes.string }; */
}
